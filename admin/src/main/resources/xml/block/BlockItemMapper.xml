<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.admin.block.item.mapper.BlockItemMapper">
    <insert id="insertBlockItem" parameterType="com.example.admin.block.item.dto.InsertBlockItemDto">
        INSERT INTO TB_BLOCK_ITEM(ITEM_NM, REG_DT, REG_ID, DCB)
        VALUES (#{product}, DATE_FORMAT(NOW(), '%Y-%m-%d %H:%i:%s'), #{mbrId}, #{dcb})
    </insert>

    <select id="getBlockItemList" parameterType="map" resultType="com.example.admin.block.item.dto.BlockItem">
        SELECT * FROM TB_BLOCK_ITEM
        WHERE DCB = #{dcb}
              <if test="item != null and item != ''">
        AND ITEM_NM LIKE CONCAT('%', #{item}, '%')
        </if>
        ORDER BY ITEM_NM
    </select>

    <delete id="deleteBlockItem" parameterType="map">
        DELETE FROM TB_BLOCK_ITEM
        WHERE DCB = #{dcb} AND ITEM_NO = #{itemNo}
    </delete>
</mapper>