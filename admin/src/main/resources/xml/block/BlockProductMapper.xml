<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.admin.block.product.mapper.BlockProductMapper">
    <insert id="insertBlockProduct" parameterType="com.example.admin.block.product.dto.InsertBlockProductDto">
        INSERT INTO TB_BLOCK_PRODUCT(PRODUCT_NAME, REG_DT, REG_ID, DCB)
        VALUES (#{product}, DATE_FORMAT(NOW(), '%Y-%m-%d %H:%i:%s'), #{mbrId}, #{dcb})
    </insert>

    <select id="getBlockProductList" parameterType="map" resultType="com.example.admin.block.product.dto.BlockProduct">
        SELECT * FROM TB_BLOCK_PRODUCT
        WHERE DCB = #{dcb}
              <if test="product != null and product != ''">
        AND PRODUCT_NAME LIKE CONCAT('%', #{product}, '%')
        </if>
        ORDER BY PRODUCT_NAME
    </select>

    <delete id="deleteBlockProduct" parameterType="map">
        DELETE FROM TB_BLOCK_PRODUCT
        WHERE DCB = #{dcb} AND PRODUCT_NO = #{productNo}
    </delete>
</mapper>