<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.admin.repository.mapper.board.BoardMapper">
    <!-- 전체 게시판 목록 조회 -->
    <select id="getBoardList" resultType="com.example.admin.domain.entity.board.Board">
        SELECT BOARD_ID, BOARD_NAME, CATEGORY_TYPE, BOARD_DEPTH, PARENT_BOARD_ID
        FROM BOARD
    </select>



    <select id="insertBoard" parameterType="com.example.admin.domain.dto.board.AddBoardDto" resultType="com.example.admin.domain.entity.board.Board">
        INSERT INTO BOARD(board_name, category_type, board_depth, parent_board_id)
        VALUES (#{boardName}, #{categoryType}, #{boardDepth}, #{parentBoardId})
    </select>


    <select id="duplicateBoard" parameterType="com.example.admin.domain.dto.board.AddBoardDto" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM BOARD
        WHERE BOARD_NAME = #{boardName} AND
              BOARD_DEPTH = #{boardDepth} AND
              PARENT_BOARD_ID = #{parentBoardId}
    </select>
</mapper>
