<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.admin.repository.mapper.item.ItemStatsDailyMapper">
    <insert id="insertItemStatDaily" parameterType="com.example.admin.domain.entity.item.ItemStatsDaily">
        INSERT INTO TB_ITEM_STATS_DAILY(YEAR, MONTH, DAY, MERCHANT_NAME, ITEM_NAME, SUM_ITEM_PRICE, SUM_TAX, SUM_TOTAL)
        VALUES (#{year}, #{month}, #{day}, #{merchantName}, #{itemName}, #{sumItemPrice}, #{sumTax}, #{sumTotal})
    </insert>

    <select id="getItemDayStats" parameterType="map" resultType="com.example.admin.domain.entity.item.ItemStatsDaily">
        SELECT * FROM TB_ITEM_STATS_DAILY
        WHERE 1 = 1
        <if test="merchantName != null and merchantName != ''">
            AND MERCHANT_NAME LIKE CONCAT('%', #{merchantName}, '%')
        </if>
        <if test="itemName != null and itemName != ''">
            AND ITEM_NAME LIKE CONCAT('%', #{itemName}, '%')
        </if>
        <if test="year != null">
            AND YEAR = #{year}
        </if>
        <if test="month != null">
            AND MONTH = #{month}
        </if>
    </select>
</mapper>